# 巴别塔（回译）：安装与保留数据的更新

## 为什么以前每次都要重新导词典

浏览器把扩展的设置、密钥、草稿和 MDX 词典索引保存在扩展 ID 对应的本地存储与 IndexedDB 中。把新版 ZIP 解压到**新目录**再加载，可能产生新的扩展 ID；新扩展看不到旧扩展的数据。PR 合并和 GitHub Release 也不会自动更新正在使用的开发者模式扩展。

## 首次安装／从旧版迁入固定目录

从 [Releases](https://github.com/Kevin-Yixuan/babel-tower-backtranslation/releases) 下载版本号最高的 ZIP，解压后确认 `manifest.json` 在所选文件夹的第一层。Chrome 打开 `chrome://extensions`、Edge 打开 `edge://extensions`，开启开发者模式，点「加载已解压的扩展程序」。这个文件夹就是以后一直使用的**固定安装目录**，不要每版换一个文件夹。

如果已经装有旧版并希望保留本地数据，先在旧扩展导出草稿和收藏作备份，确认旧扩展加载的真实文件夹路径。把新版**运行文件覆盖进旧文件夹**，不要删掉旧扩展或旧文件夹；在扩展管理页对同一个扩展点一次「重新加载」，再刷新 X。0.2.2 及更早版本没有自动重载机制，因此首次迁入新版需要这一次手工操作。旧词典索引在浏览器配置文件中，更新安装目录里的源码不需要重新导入 MDX。

如果已经从不同目录装出了两个扩展 ID，不要删除旧版。当前没有跨 ID 自动迁移工具；先保留旧版与备份，确认数据后再决定如何迁移。

## 一次设置，以后自动跟进合并后的版本（Windows）

在包含 `update-unpacked.ps1` 和 `update-marker.json` 的版本已经加载后，打开 PowerShell，用**实际固定安装目录**替换以下路径：

```powershell
powershell.exe -NoProfile -File "D:\你的固定扩展目录\update-unpacked.ps1" -InstallDir "D:\你的固定扩展目录" -RegisterDailyTask -At "09:00"
```

这会建立名为 `BabelTowerBacktranslationUpdater` 的每日任务（在你登录 Windows 时运行）。它检查本项目的 GitHub Releases；有更高版本时下载 ZIP 与校验文件、核对 SHA-256、仅更新同一安装目录的运行文件、写入完成标记。扩展每隔约五分钟检查本地标记，等所有 X 标签页关闭后自行重新加载；不会动浏览器用户目录中的设置或词典数据。电脑关机或离线时，本次检查会在下次计划运行时重试。若 Windows 拒绝运行下载的脚本，先确认文件来自本项目，再在文件属性中解除阻止。

可以先只验证将要安装的版本，不覆盖文件、不创建每日任务：

```powershell
powershell.exe -NoProfile -File "D:\你的固定扩展目录\update-unpacked.ps1" -InstallDir "D:\你的固定扩展目录" -DryRun
```

任务不会覆盖同名的已有计划任务。要停止自动检查，可在 Windows「任务计划程序」中禁用或删除 `BabelTowerBacktranslationUpdater`；扩展及其本地数据仍会保留。

## 面向社群的大规模分发

此 GitHub 更新器适用于愿意在 Windows 上运行本地脚本的开发者模式用户。Chrome／Edge 商店安装的扩展由浏览器处理自动更新，普通用户不必保留源码目录或运行脚本；要做到这一步，仍需分别完成商店上架与真实 X／模型验收。无论哪种渠道，**保持同一个扩展 ID** 才能沿用原配置与词典索引。变更权限时浏览器可能要求用户再次确认。

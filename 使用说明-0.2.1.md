# 巴别塔（回译）0.2.1 社群安装与升级说明

## 首次安装

1. 下载 `babel-tower-backtranslation-0.2.1.zip`，解压到一个固定目录；`manifest.json` 应直接位于该目录下。
2. Chrome：打开 `chrome://extensions`；Edge：打开 `edge://extensions`。开启「开发者模式」，点「加载已解压的扩展程序」，选择该目录。ZIP 需要先解压，不能把 ZIP 本身当文件夹选择。
3. 打开或刷新 `x.com`。帖子下有「翻译」「回复」按钮，右侧小把手可打开侧栏。
4. 点击扩展图标，在「开始」页选择模型服务商、模型名并填写自己的 API Key。可主动点「测试连接」，它会调用一次真实模型。

## 从 0.1.0 或其他已安装版本升级

**请先备份，且不要先移除旧扩展。** 在旧版弹窗导出收藏 JSON，在自由写作工作台导出全部草稿 JSON，并记下自己填写的服务商、规则与词典来源。导出文件供备份核对，当前版本没有跨扩展 ID 的一键导入功能。

若要保留浏览器本地数据，应把新版运行文件更新到**旧版已经加载的同一目录**，在扩展管理页点该扩展的「重新加载」，再刷新 X。不要把 0.2.1 解压到新目录并卸载旧版：目录变化可能产生新的扩展 ID，新扩展无法直接读取旧扩展的密钥、收藏、草稿和词典索引。对旧目录的文件操作请由你自己在确认备份后进行；本发行流程没有改动你的安装目录。

如果已经装到了新目录，请保留旧扩展与旧目录，先核对两边数据，不要删除旧版。

## 使用与注意

- 打开「翻译」页签或划选句子进入阅读后，会自动把当前帖文或文章段落送往配置的模型，可能消耗额度；缺少密钥时会提示配置。
- Jev 折叠与兴趣高亮默认关闭。开启后会把附近帖子文字及规则直接发送给 TypeSafe，包括你有权查看的受保护账号帖子；每日上限按实际请求数计算，含失败和重试。
- 插入回复前会显示目标确认。无法证明编辑框属于正确帖子时只提供复制；扩展不会自动点击 X 的发布按钮。
- 中文回译、风险提示和成长记忆依赖模型，可能有误。请检查草稿里的事实、身份、立场、链接及语气，再自行决定是否发布。
- 真实 X 界面结构会变化；若侧栏遮挡内容、翻译漏段或回复目标不明确，请关闭侧栏或改用复制，并向项目反馈去敏后的复现步骤。
